# FDE MCP Blade —— 环境变量模板
# install.sh 会自动替换下方占位符并生成 .env；手工部署请复制为 .env 后自行修改。

# ── 必填：JWT 签名密钥（install.sh 自动生成；手工部署生成方式：openssl rand -base64 32）──
JWT_SECRET="your-super-secret-key-that-is-at-least-32-bytes-long"

# ── 数据库口令（install.sh 自动生成随机值；手工部署请务必修改默认值）──
MARIADB_ROOT_PASSWORD="your-root-password"
MARIADB_PASSWORD="your-gis-remote-password"

# ── 服务端口 ──
SERVER_PORT=8018
MQTT_PORT=1883

# ── 日志级别 ──
RUST_LOG=info

# ── 无线网卡（历史兼容项，当前镜像未启用，留空即可）──
WIRELESS_IFACE=""
